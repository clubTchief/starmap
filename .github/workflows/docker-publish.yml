name: Build and publish Docker image

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]   # gives every PR (including Dependabot's) a real build/test
                        # signal — see the "test" job below, and STARMAP Project
                        # Manual §9.5 for the incident that motivated this.
  workflow_dispatch: {}   # lets you trigger a rebuild manually from the Actions tab

env:
  REGISTRY: ghcr.io

jobs:
  # Runs on every PR targeting main, including Dependabot's. Builds through
  # the Dockerfile's orekit-prep and builder stages ONLY — the exact same
  # steps a real deploy build runs, including the full 15-test suite against
  # real Orekit data — but never reaches the runtime stage and never pushes
  # or deploys anything. This closes the gap where Dependabot PRs previously
  # opened with no automatic signal at all, since build-and-push below only
  # ever triggered on push. A failing PR here doesn't block merging on its
  # own unless branch protection is also enabled to require this check.
  test:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Log in to Docker Hub (avoids anonymous pull rate limits)
        if: ${{ vars.DOCKERHUB_USERNAME != '' }}
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build through test stage (no push, no deploy)
        uses: docker/build-push-action@v7
        with:
          context: .
          target: builder
          push: false
          no-cache: true

  build-and-push:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # needed to push to GHCR

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # Docker/GHCR tags must be lowercase, but the repo is clubTchief/starmap
      # (capital T), so github.repository can't be used as-is.
      - name: Compute lowercase image name
        run: echo "IMAGE_NAME=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Every base image in the Dockerfile (maven:3.9-eclipse-temurin-17-alpine,
      # eclipse-temurin:17-jre-alpine, alpine:3.19) pulls from Docker Hub, not
      # GHCR. GitHub Actions' shared runner IPs frequently hit Docker Hub's
      # anonymous pull rate limit (100 pulls/6hr per IP), which shows up as a
      # silent hang at "load metadata for docker.io/..." rather than a clean
      # error — exactly what happened on 2026-08-16. Authenticating with a
      # free Docker Hub account raises that limit substantially. Optional:
      # if DOCKERHUB_USERNAME/DOCKERHUB_TOKEN secrets aren't set, this step
      # is skipped entirely (the build just goes back to anonymous pulls)
      # rather than failing the whole pipeline.
      - name: Log in to Docker Hub (avoids anonymous pull rate limits)
        if: ${{ vars.DOCKERHUB_USERNAME != '' }}
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push (no cache — always fresh)
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          no-cache: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:production
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

      # Closes the loop that used to be manual: push -> wait for Actions ->
      # copy the new SHA -> paste into Railway -> redeploy, by hand, every
      # time. Railway's service should be configured to track the
      # ":production" tag (a stable, non-"latest" tag Railway won't try to
      # auto-manage) — this step tells Railway to redeploy that tag right
      # now instead of waiting for its own auto-update detection, which can
      # take hours. Requires a RAILWAY_TOKEN repo secret (Railway dashboard
      # → project → Settings → Tokens → create a project token) and the
      # exact service name below to match your Railway service.
      - name: Trigger Railway redeploy
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: npx -y @railway/cli@latest redeploy --service starmap --yes
        continue-on-error: true

      # Every push publishes a new :<sha> tag that never gets deleted on its
      # own — harmless at first, but it quietly accumulates in GHCR forever.
      # Keep the most recent 15 untagged/sha-tagged versions and let the
      # rest go; :latest and :production are excluded by name so they're
      # never at risk of deletion here.
      #
      # NOTE: package deletion sometimes needs a personal access token with
      # delete:packages scope rather than the default GITHUB_TOKEN, depending
      # on org/repo package permission settings. If this step fails with a
      # 403, create a PAT, add it as a GHCR_CLEANUP_TOKEN secret, and swap
      # secrets.GITHUB_TOKEN below for secrets.GHCR_CLEANUP_TOKEN.
      - name: Clean up old image versions
        uses: actions/delete-package-versions@v5
        with:
          package-name: starmap
          package-type: container
          min-versions-to-keep: 15
          ignore-versions: '^(latest|production)$'
        continue-on-error: true
